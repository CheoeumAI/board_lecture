Material 3 Checkbox — for selecting one or more items, or toggling a single option.

```jsx
<Checkbox label="이용약관에 동의합니다" defaultChecked />
<Checkbox indeterminate label="전체 선택" />
```

Props: `checked`/`defaultChecked`, `indeterminate`, `error`, `disabled`, `label`, `onChange(checked)`.
