Material 3 Radio Button — pick exactly one option from a mutually-exclusive set.

```jsx
<RadioButton name="plan" value="free" label="무료" defaultChecked />
<RadioButton name="plan" value="pro" label="프로" />
```

Props: `checked`/`defaultChecked`, `name`, `value`, `label`, `disabled`, `onChange(value)`.
