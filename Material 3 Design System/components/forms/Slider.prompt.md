Material 3 Slider — choose a value from a continuous range (volume, brightness, etc.).

```jsx
<Slider defaultValue={40} showValue onChange={(v) => setVolume(v)} />
```

Props: `value`/`defaultValue`, `min`, `max`, `step`, `showValue` (value bubble), `disabled`, `onChange(value)`.
