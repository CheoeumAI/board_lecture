Material 3 Badge — small overlay showing a count or a status dot, anchored to an icon.

```jsx
<Badge count={4}><IconButton icon="notifications" /></Badge>
<Badge dot><IconButton icon="mail" /></Badge>
```

Props: `count`, `max` (default 999), `dot`, `children` (anchor; omit for standalone).
